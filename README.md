# bot

بوت Discord مُحسّن من ناحية **الـ UI** مع أوامر داخل **Panel موحّدة** فيها اسم الأغنية، المصدر، الأزرار، وحالة التشغيل.

## الجديد الآن ✅
- 🎧 **ربط صوتي كامل**: دخول روم الصوت تلقائياً عند `!play` أو `!video`.
- ▶️ **تشغيل فعلي** للمقطع الصوتي من روابط YouTube الناتجة.
- ⏭️ **Skip حقيقي** مع الانتقال للتراك التالي تلقائياً.
- 🔁 **Queue runner**: عند انتهاء الأغنية ينتقل تلقائياً للعنصر التالي.
- ⏹️ **Stop حقيقي**: إيقاف التشغيل، مسح القائمة، والخروج من الاتصال الصوتي.

## التحسينات
- 🎛️ كل الأوامر الأساسية ترجع داخل **لوحة Embed** موحدة بدل الردود النصية المبعثرة.
- 😀 إيموجيز جديدة في كل أوامر التشغيل والتحكم.
- 🎵 نظام تشغيل موسيقى/فيديو مبني على:
  - **YouTube Data API** للبحث عن الفيديوهات.
  - **Spotify metadata API** لقراءة روابط Spotify وتحويلها لبحث متوافق مع يوتيوب.
- 🔘 أزرار تحكم محسّنة (Pause/Resume - Skip - Stop) بألوان واضحة ومربوطة مباشرة بمنطق المشغّل.

## الأوامر
- `!help` → لوحة الأوامر.
- `!play <song|url>` → لوحة تشغيل تحتوي اسم الأغنية + المصدر + الأزرار.
- `!video <query>` → نفس لوحة التشغيل لكن من بحث فيديو.
- `!queue` → لوحة قائمة التشغيل الحالية + أزرار التحكم.
- `!skip` → تحديث اللوحة إلى الأغنية التالية.
- `!stop` → لوحة تأكيد الإيقاف والمسح.

## الإعداد
1. أنشئ ملف `.env`:

```env
DISCORD_TOKEN=...
DISCORD_CLIENT_ID=...
YOUTUBE_API_KEY=...
SPOTIFY_CLIENT_ID=...
SPOTIFY_CLIENT_SECRET=...
BOT_ACCENT_COLOR=3066993
```

2. ثبّت الحزم:

```bash
npm install
```

3. شغّل البوت:

```bash
npm start
```

## ملاحظات
- تم الاستغناء عن أي اعتماد على `ytdb` واستبداله بمزودات API مباشرة لنتائج أدق.
- عند استخدام `!play`/`!video` يجب أن يكون المستخدم داخل قناة صوتية.
